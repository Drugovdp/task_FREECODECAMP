/*Write a function that returns both the minimum and maximum number of the given list/array.*/
function minMax(arr) {
    let arrRes = [];
    arrRes.push(Math.min.apply(null, arr));
    arrRes.push(Math.max.apply(null, arr));
    return arrRes; // fix me!
}
